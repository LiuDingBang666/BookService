<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
  http://www.springframework.org/schema/beans/spring-beans-4.3.xsd 
  http://www.springframework.org/schema/mvc 
  http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd 
  http://www.springframework.org/schema/context 
  http://www.springframework.org/schema/context/spring-context-4.3.xsd">
    <!-- 配置包扫描 -->
    <context:component-scan
            base-package="icu.liuwisdom.core.controller"/>
    <!-- 配置注解驱动 -->
    <mvc:annotation-driven/>
    <!--配置静态资源的访问映射，此配置中的文件，将不被前端控制器拦截 -->
    <!-- <mvc:resources location="/user/js/" mapping="/user/js/**"/>
     <mvc:resources location="/user/css/" mapping="/user/css/**"/>
     <mvc:resources location="/user/images/" mapping="/user/images/**"/>

     <mvc:resources location="/admin/js/" mapping="/admin/js/**"/>
     <mvc:resources location="/admin/css/" mapping="/admin/css/**"/>
     <mvc:resources location="/admin/images/" mapping="/admin/images/**"/>
     <mvc:resources location="/admin/img/" mapping="/admin/img/**"/>
     <mvc:resources location="/admin/font-awesome/" mapping="/admin/font-awesome/**"/>-->
    <mvc:default-servlet-handler></mvc:default-servlet-handler>
    <!-- 定义视图解析器 -->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!-- 设置前缀 -->
        <property name="prefix" value="/WEB-INF/"/>
        <!-- 设置后缀 -->
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/><!--过滤所有请求-->
            <mvc:exclude-mapping path="/common/**/**"/><!--配置指定资源不拦截-->
            <bean class="icu.liuwisdom.core.interceptor.AdminLoginInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**"/><!--过滤所有请求-->
            <mvc:exclude-mapping path="/common/**/**"/><!--配置指定资源不拦截-->
            <bean class="icu.liuwisdom.core.interceptor.UserLoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>